<% if customer_signed_in? %>
	<h2>マイページ</h2>

	<h2>登録情報</h2>
	<%=link_to "編集", edit_customer_path(@customer) %>
	<%= link_to "パスワードを変更する", customer_edit_password_path %>
		<h4>名前</h4>
			<%= @customer.last_name %><%= @customer.first_name %>
		<h4>カナ</h4>
			<%= @customer.last_name_kana %><%= @customer.first_name_kana %>
		<h4>郵便番号</h4>
			<%= @customer.customers_postal_code %>
		<h4>住所</h4>
			<%= @customer.customers_address %>
		<h4>電話番号</h4>
			<%= @customer.phone_number %>
		<h4>メールアドレス</h4>
			<%= @customer.email %>


		<h2>配送先</h2>
		<%= link_to '配送先', deliver_infos_path %>
		<h2>注文履歴</h2>



	 	<br><%= link_to "logout", destroy_customer_session_path, method: :delete %>
<% end %>